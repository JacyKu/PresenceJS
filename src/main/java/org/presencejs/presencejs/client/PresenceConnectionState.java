package org.presencejs.presencejs.client;

public enum PresenceConnectionState {
    IDLE,
    DISABLED,
    CONNECTING,
    READY,
    DISCORD_NOT_FOUND,
    INVALID_CLIENT_ID,
    DISCONNECTED,
    ERROR
}

